name: Dredd

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: [linux]
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@v2

    - name: Testing
      run:  make test
